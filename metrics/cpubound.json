{
  "name": "cpubound",
  "description": "Identical CPU-bound handler (~5 ms busy-spin per invocation) on each platform; the SAQEF protocol. Runs are COUNT-BOUND (exactly total requests); duration is a safety cap only.",
  "defaults": {
    "total": 3000,
    "concurrency": 20,
    "duration": 60,
    "warmup": 20,
    "repeat": 5,
    "slo_ms": 500.0,
    "sampler": "cgroup",
    "loadgen": "hey",
    "delta_check": true,
    "verify_n": 100,
    "verify_budget_ms": 5.0
  },
  "regression": {
    "description": "c=4, 8-core bare metal, REPEAT=5, calibrated IDLE_W=4.3. Must reproduce the known-good medians (fn 11.60 / openfaas 7.67) within tolerance_pp, or the refactor is NOT trusted. OpenFaaS runs first, torn down (stack rm + service rm hello), then Fn.",
    "reference_notes": "RE-ANCHORED 2026-08-09 (2026-08-08T19:46:49Z) from same-day old-runner A/B on the quiet box under the self-certifying ambient-load gate: fn=11.49 (outdir results/fn_cpubound_crosscheck_2026-08-09), openfaas=7.61 (outdir results/of_cpubound_crosscheck_2026-08-09). Supersedes the 2026-08-06 references. Box-state drift, not a refactor change (old-runner A/B per TROUBLESHOOTING_RUNBOOK.md \u00a72/\u00a73).",
    "concurrency": 4,
    "total": 10000,
    "duration": 60,
    "warmup": 20,
    "repeat": 5,
    "idle_w": 4.3,
    "replicas_openfaas": 16,
    "reference_share_pct": {
      "fn": 11.49,
      "openfaas": 7.61
    },
    "tolerance_pp": 0.5
  }
}